<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>{{ title }}</title>
    {{! Files will be fingerprinted for cache busting }}
    {{! Access their filenames with the fingerprint variable }}
    <link rel="stylesheet" href="{{ fingerprint.[css/main.css] }}">
</head>

<body>

{{> navigation }}

{{{ contents }}}

{{! Each file has a filename variable }}
<p>This file's filename was: {{ filename }}</p>

{{path}}

<script src="{{ fingerprint.[js/main.js] }}"></script>

</body>
</html>